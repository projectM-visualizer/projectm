bin_PROGRAMS = gst-projectM

# sources used to compile this plug-in
gst_projectM_SOURCES = gstprojectM.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_projectM_CFLAGS = $(GST_CFLAGS)
gst_projectM_LDADD = $(GST_LIBS)

