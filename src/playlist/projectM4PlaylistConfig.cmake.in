set(projectM4Playlist_VERSION @PROJECT_VERSION@)

@PACKAGE_INIT@

if("@ENABLE_BOOST_FILESYSTEM@") # ENABLE_BOOST_FILESYSTEM
    find_dependency(Boost COMPONENTS Filesystem)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/projectM4PlaylistTargets.cmake")
