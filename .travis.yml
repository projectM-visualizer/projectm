language: cpp

addons:
  apt:
    packages:
      - libsdl2-dev
      - libglew-dev
      - libftgl-dev
      - libsdl2-dev
      - libdevil-dev
      - g++-7
    sources:
      - ubuntu-toolchain-r-test

before_script: autoreconf --install
